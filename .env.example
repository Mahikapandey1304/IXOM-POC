# IXOM-POC Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# REQUIRED CONFIGURATION
# ============================================

# OpenAI API Key (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-YOUR_ACTUAL_OPENAI_API_KEY_HERE

# ============================================
# OPTIONAL CONFIGURATION (with defaults)
# ============================================

# OpenAI Model Selection
# Options: gpt-4o (recommended), gpt-4o-mini (cheaper), gpt-4-turbo, gpt-4.1
DEFAULT_MODEL=gpt-4o

# LLM Temperature (0 = deterministic, 1 = creative)
TEMPERATURE=0

# Directory Paths (relative to project root)
DATA_DIR=data
LOGS_DIR=logs
OUTPUTS_DIR=outputs

# PDF Rendering Configuration
IMAGE_DPI=200
MAX_IMAGE_SIZE=2048
MAX_PAGES_PER_DOC=10

# ============================================
# PRODUCTION SETTINGS (for deployment)
# ============================================

# Uncomment and set these for production deployment
# SERVER_PORT=8501
# SERVER_ADDRESS=0.0.0.0
# ENABLE_CORS=false
# ENABLE_XSRF_PROTECTION=false

# ============================================
# NOTES
# ============================================
# - Never commit the actual .env file to git (it's in .gitignore)
# - Keep your OpenAI API key secure and rotate it regularly
# - For production, consider using environment variables instead of .env file
# - Monitor your OpenAI API usage at: https://platform.openai.com/usage
